First, run the Data_Split code to split the array of data into Training/Validating and Testing.

Second, run the Create_Pairs code to create the positive and negative pairs and their labels.

Third, run the Convert_Tensor code to convert all the paths in the positive and negative arrays to tensors. 
